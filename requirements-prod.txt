# Production requirements (runtime with OpenAI embeddings API)

# FastAPI and Server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# OpenAI Integration (for embeddings API - no local ML needed!)
openai>=1.3.0

# Database
aiosqlite>=0.19.0
sqlite-vec>=0.1.0

# Utilities
tenacity>=8.2.3
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
numpy>=1.24.0

# NOTE: sentence-transformers and torch NOT needed
# Using OpenAI Embeddings API instead (text-embedding-3-small)
